# Credentials — NEVER commit these
.env

# Output & uploads
output/
attachments/
logs/
data/
uploads/
jobs/

# Local test-run harness — holds large customer PDFs/outputs, never deploy
*_local_run/

# Python
*.pyc
__pycache__/
venv/
.venv/

# macOS
.DS_Store

# Streamlit secrets (for local dev)
.streamlit/secrets.toml

# IDE
.vscode/
.idea/

# Deprecated / backup files
DEP *.py
*.backup
*.backup2
*.save
config.py.save

# Cache
.cache/
.claude/

# Submissions (uploaded via web_app)
submissions/

# Local DB backup dumps
nightshift_pre_*.sql
*.sql.bak
spike_samples/

# Local validation artifacts (need API credits to run)
validate_recovery.log
validate_recovery_summary.json
*_validate.json
*_validate.pdf
*_validate_run*.json
*_validate_run*.pdf
*_persheet.json
*_persheet.pdf
*_persheet_fixed.json
*_residuals.json
*.log

# Validation-run artifacts (nsai_* batches, golden plan sets).
# The harness SCRIPTS and the analysis NOTES are committed; the customer bid
# sets, run outputs and logs are not — they are third-party confidential
# documents and would bloat history permanently (~600MB as of 2026-09-01).
golden/plans/
nsai_*/**/*.pdf
nsai_*/**/*.json
nsai_*/**/*.log
nsai_*/**/*.txt
nsai_*/**/*.xlsx
nsai_*/*.pdf
nsai_*/*.json
nsai_*/*.log
nsai_*/*.txt
nsai_*/*.xlsx
nsai_*/*.html
